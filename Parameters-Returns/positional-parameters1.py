#pass arguments in order — the first one goes to the first parameter, the second to the second,
def calculate_area(length,width):
    return length * width

#call it using positions
print(calculate_area(10,5))